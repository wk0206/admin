"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = void 0;

function _react() {
  const data = _interopRequireDefault(require("react"));

  _react = function _react() {
    return data;
  };

  return data;
}

function _path() {
  const data = _interopRequireDefault(require("path"));

  _path = function _path() {
    return data;
  };

  return data;
}

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

/**
 * webpack plugin for add extra sideEffects item if user configured sideEffects in package.json
 */
class docSideEffectsWebpackPlugin {
  constructor(opts) {
    this.opts = opts;
  }

  apply(compiler) {
    compiler.hooks.normalModuleFactory.tap(this.constructor.name, normalModuleFactory => {
      normalModuleFactory.hooks.afterResolve.tap(this.constructor.name, data => {
        var _data$resourceResolve;

        const descriptionFileData = (_data$resourceResolve = data.resourceResolveData) === null || _data$resourceResolve === void 0 ? void 0 : _data$resourceResolve.descriptionFileData;
        const sideEffectsFlag = descriptionFileData === null || descriptionFileData === void 0 ? void 0 : descriptionFileData.sideEffects;

        if ((sideEffectsFlag === false || Array.isArray(sideEffectsFlag)) && _path().default.normalize(data.resourceResolveData.descriptionFilePath) === this.opts.pkgPath) {
          const list = new Set(sideEffectsFlag || []);
          this.opts.sideEffects.forEach(item => list.add(item));
          descriptionFileData.sideEffects = Array.from(list);
        }
      });
    });
  }

}

exports.default = docSideEffectsWebpackPlugin;